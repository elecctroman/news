RewriteEngine On

# Mevcut dosya veya klasörlere doğrudan erişimi koru.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Diğer tüm istekleri public dizinine yönlendir.
RewriteRule ^(.*)$ public/$1 [L]

FallbackResource /public/index.php
